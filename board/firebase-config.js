/**
 * Firebase 설정값.
 * Firebase 콘솔 > 프로젝트 설정(⚙️) > 내 앱 > 웹 앱 에서 복사한 값을 그대로 붙여넣으세요.
 * 이 값들은 브라우저에 공개되는 식별자라 비밀번호가 아닙니다.
 * 실제 보안은 firestore.rules 의 규칙이 담당합니다.
 */
export const firebaseConfig = {
  apiKey: "",
  authDomain: "",
  projectId: "",
  storageBucket: "",
  messagingSenderId: "",
  appId: "",
};
